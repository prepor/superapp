# The Dev menu's toggle: ⇧⌘L puts the panels library up over the running
# workspace, and takes it down again. Run as a workspace suite (no
# --library): the script stays the stage's, and keeps ticking underneath.
wait 900
key cmd+shift+l
wait 900
shot dev-01-library
key cmd+shift+l
wait 600
shot dev-02-workspace
quit
