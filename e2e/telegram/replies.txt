# args:
# env:
# Replies have their own count and inbox, even when the group is muted.
wait 900
key cmd 2
wait 500
type "chats"
wait 500
key enter
wait 900
shot replies-list
click "replies & mentions 2"
wait 900
shot replies-inbox
click "I saw it — the inset is 28 dp, it wants 34"
wait 900
# The preview reveals and reads the reply while the inbox keeps focus.
visible "Vera Kovac: I saw it — the inset is 28 dp, it wants 34"
shot replies-open
click "refresh"
wait 500
shot replies-read
key cmd+left
wait 500
click "replies & mentions 0"
wait 500
quit
