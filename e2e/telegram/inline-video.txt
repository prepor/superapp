# Play stays in the transcript. Only the video picture opens the viewer.
wait 900
key cmd 2
wait 500
type "chats"
wait 500
key enter
wait 900
click "stelaxis"
wait 900
# The demo video precedes the unread run the chat opens on.
click "me: the muted grey is too light on the fold"
key up
wait 300
click "play video"
wait 1200
click "write a message"
click "0:01 / 0:14"

# Selecting and marking a playing message keeps its player.
# The cursor is on the video; leave the composer to mark that row.
key esc
wait 300
key space
wait 300
click "pause video"
wait 300
shot telegram-inline-video

# The picture opens the viewer, whose timeline is addressable there.
click "video"
wait 900
click "seek"
key cmd+w
wait 900
click "play video"
wait 300
click "pause video"
click "write a message"

# Opening a card while the transcript plays must still allow playback
# handoffs in both directions, with only one panel playing at a time.
key esc
wait 300
click "play video"
wait 300
click "line"
wait 900
click "play in card"
wait 300
click "play video"
wait 300
click "play in card"
wait 1200
click "pause in card"
wait 300
click "0:01 / 0:14"
click "video"
wait 900
click "seek"
quit
