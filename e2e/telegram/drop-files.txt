# Desktop drops go through Makepad's drag/drop events, using real local files.
wait 900
key cmd 2
wait 400
type "chats"
wait 400
key enter
wait 700
click "Vera Kovac"
wait 700
# A rejected attachment must have a persistent visible error and dismissal.
dropfiles ["write a message", "/nonexistent/telegram-drop-test.png"]
wait 500
click "attaching file failed"
wait 300
shot dropped-file-error
click "dismiss"
wait 300
dropfiles ["write a message", "app/resources/icon_32.png", "README.md"]
wait 500
click "icon_32.png · photo"
wait 400
click "README.md · file"
wait 400
# Dropping the same path again must retain one attachment for that file.
dropfiles ["write a message", "app/resources/icon_32.png"]
wait 400
click "write a message"
type "files from a desktop drop"
key enter
wait 500
shot dropped-files-sent
quit
