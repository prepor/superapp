# The settings walk: reach it via the launcher, fill the form with the
# tab/enter walk (an .invalid host so the worker fails fast and locally),
# submit with enter past the last field, watch the status land via the
# worker's signal, remove the account again.
wait 700
key cmd 2
wait 400
type "settings"
wait 300
key enter
wait 600
shot s1-settings
click "address"
type "andrey@fastmail.test"
key tab
type "app-pass-123"
key tab
key backspace 30
type "imap.invalid"
key enter
wait 200
shot s2-filled
key enter
wait 2500
shot s3-account-error
click "remove"
wait 500
shot s4-removed
quit
