# The HTML reading of a mail: the letter arrives, the layout does not.
wait 900
shot 01-boot

# The GitHub notification is the demo's one HTML sender.
click "CI failed"
wait 800
shot 02-html-body

# A plain-text sender still takes the plain path, in the same panel slot.
click "Q3 infra"
wait 800
shot 03-plain-body

# And back again. This is the shot that matters: one reading must replace
# the other, not stack on top of it. Hiding a widget that carries no
# `visible` is a silent no-op, and the first version of this read as
# correct only because the plain run had never been given text yet.
click "CI failed"
wait 800
shot 03b-back-to-html

# Selection over the HTML body. cmd+a is not available to assert with here
# the way e2e/select.txt uses it: in a message panel that chord is archive,
# so it never reaches the widget, and a synthesized drag never selects. So
# the run is selected on the widget itself: the shot is the wash — every
# line washed, and no darker band where two lines meet.
click "CI failed"
wait 800
drag "mail html" 380 0
wait 400
selectall "mail html"
wait 400
shot 04-html-selected

quit
