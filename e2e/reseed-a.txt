# Holder: bootstrap + seed the demo world, open a reply compose, type the
# draft "alpha", let it publish. Quits (releasing the lease).
wait 1500
click "Q3 infra"
wait 500
click "reply"
wait 800
type "alpha"
wait 3000
shot rs-a-alpha
quit
