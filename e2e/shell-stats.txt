# args:
# env:
# The stats root, its storage and activity values, and both refresh routes.

wait 600
key cmd 2
wait 400
type "stats"
wait 500
key enter
wait 900
click "stats database"
click "stats file cache"
click "stats cached files"
click "stats open panels"
click "stats queued jobs"
click "stats failed jobs"
shot stats-open

click "refresh"
wait 600
click "stats database"
click "stats file cache"
shot stats-refreshed

# Escape gives the panel its keys after selecting a value.
key esc
key r
wait 600
click "stats failed jobs"
shot stats-keyboard
quit
