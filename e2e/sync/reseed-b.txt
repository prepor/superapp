# Follower: installs A's lineage — the compose panel appears seeded "alpha",
# hidden behind the 72%-opaque lock. A peer then edits the draft to
# "alpha beta"; a running follower must re-seed its retained compose widget
# from the materialized row. Taking over lifts the lock and reveals what the
# widget holds: "alpha beta" only if the re-seed fired ("alpha" is the stale
# buffer).
wait 3000
shot rs-b1-locked
wait 60000
click "take over"
wait 2500
shot rs-b2-takeover
quit
